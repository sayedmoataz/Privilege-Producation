import 'package:flutter/material.dart';

class MySingleCourse extends StatelessWidget {
  const MySingleCourse({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
